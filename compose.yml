name: tab-guardian

services:
    app:
        image: tab-guardian
        container_name: tab-guardian
        build:
            context: .
            dockerfile: Containerfile
        volumes:
            - .:/app:z
        ports:
            - 3000:5173
